#include<bits/stdc++.h>
using namespace std;

int main()
{
    string a;
    cin>>a;
    for(i=0;i<a.size();i++)
    {
        if(a[i]=='h')
        {
            i=p;
            for(j=p;j)
            if(a[i+1]=='h' || a[i+1]=='e')
                flag=0;
        }
        if(a[i]=='e')
        {
            for()
        }
    }
}
